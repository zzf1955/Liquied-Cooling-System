## 电池模拟与深度强化学习训练系统

这是一个简易的模拟真实环境下工业电池热管理系统及其控制算法的仓库，使用 `python-3.9` 语言进行编写，强化学习库使用 `tianshou` ，使用 `torch` 作为深度学习框架，使用 `uv` 进行项目管理，当前最新版本的分支为 `maybe_final`。

## 电池降阶环境

我们构造了一个电池降阶模型作为强化学习环境，其形式化如下：
- 状态空间: 顶部温度、底部温度和核心温度，以及运行电流和总电压
- 动作空间：冷却液入口温度和水泵流速，这些变量是实际电池热管理系统中物理上可实现的控制信号，会受到实际物理约束
- 转移方程：基于热力学方程进行热量传递。
- 控制成本：$\text{cost} = \lambda R_{\text{flow}} + \mu \left|T_{\text{inlet}}^{(t)} - T_{\text{inlet}}^{(t-1)}\right|$, 鼓励平滑且节能的调节。
- 奖励函数：我们希望惩罚超过安全边界的温度，同时不同电芯之间的温差需要最小化，控制成本也需要最小化。

**注意：对于基本的液冷电池模块，一般来说，液冷管在电池下方流经，从而带走电池生电发热**

## 强化学习算法

我们主要使用深度扩散软演员-评论家算法进行训练，其基本思路是采用扩散模型生成以当前状态为条件的潜在表示，从中解码出动作分布。相关模块代码见 [dsac.py](./dsac.py) 和 [diffusion/](./diffusion/)。

### 相关模块

- [BatteryEnv/](./BatteryEnv/)，包含电池模型，电池模拟环境
- [diffusion/](./diffusion/)，继承自AGOD，基本没有问题
- [dsac.py](./dsac.py) **dsac训练**
- [evaluate.py](./evaluate.py) **测试自定义policy**
- [fixed_action_agent.py](./fixed_action_agent.py) **从csv读确定的action训练**
- [log/](./log/) **训练log** 
- [test_log](./test_log/) **测试用log**
- [policy](./policy/) **训练完的policy**
- [sac.py](./sac.py) **sac训练**
- [test_dsac.py](./test_dsac.py) **测试dsac代码**

## 代码编写要求

每次接收到我的需求后，你需要展开 plan，不断提出不清晰的问题，直至没有不清晰的地方才能开始编写代码。

- 代码风格：遵循 `PEP8` 规范，使用 `type hint` 进行类型标注，使用 `docstring` 进行文档编写。
- 提交规范：每次提交都应该有对应的 `commit message`，描述本次提交的主要内容。
- 单元测试：编写单元测试，确保代码的正确性。
- 注释：代码中需要添加注释，但不要过多，过多的注释会让代码难以阅读。
- 每次修改均需要另起一个分支，并在完成测试后合并到主分支。
- 当代码运行失败的时候，你需要自主分析错误原因，并尝试解决，最后将解决方式写在 [PROGRESS.md](./PROGRESS.md) 内。

## 经验教训沉淀

每次遇到问题或完成重要改动后，要在 [PROGRESS.md](./PROGRESS.md) 中记录：
- 遇到了什么问题
- 如何解决的
- 以后如何避免
- **必须附上 git commit ID**

**同样的问题不要犯两次！**

